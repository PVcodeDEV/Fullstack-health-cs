# openspec/config.yaml
# Auto-generated by sdd-init — 2026-05-29
schema: spec-driven

context: |
  Project: ERP Clinico — medical clinic management system for a ~40-employee clinic in Peru
  Stack: Java 25 + Spring Boot 4 + PostgreSQL 18
  Frontend: Thymeleaf + HTMX + Tailwind CSS
  Server: HPE ProLiant MicroServer Gen11, Windows Server 2022, 16 GB RAM
  Clients: Windows 11, 8 GB RAM, browser-only
  Architecture: Modular monolith (redesign from scratch, not evolution)
  Modules: seguridad, core-clinico (admision, hospitalizacion, SOP), farmacia/almacen, caja/facturacion, rrhh-basico
  Testing: Not yet configured — project is greenfield (no build files, no test runner)
  Style: Spring Boot conventions, Thymeleaf server-side rendering, HTMX for interactivity

strict_tdd: false

rules:
  proposal:
    - Include risk assessment per module
    - State impact on clinic operations during rollout
  specs:
    - Use Given/When/Then for scenarios
    - Use RFC 2119 keywords (MUST, SHALL, SHOULD, MAY)
    - Include data privacy requirements (Ley de Protección de Datos Personales — Peru)
  design:
    - Include module dependency diagrams
    - Document architecture decisions with rationale
    - Consider Windows Server deployment constraints
  tasks:
    - Group by module phase
    - Keep tasks completable in one session
    - Reference Java 25 / Spring Boot 4 specific patterns
  apply:
    - Follow Spring Boot project conventions
    tdd: false
    test_command: mvn test
  verify:
    test_command: mvn test
    build_command: mvn compile
    coverage_threshold: 0
  archive:
    - Warn before merging destructive deltas

testing:
  runner: maven
  runner_framework: junit5
  layers:
    unit:
      available: true
      tool: JUnit 5 + Mockito (via spring-boot-starter-test)
    integration:
      available: true
      tool: Spring Boot Test (via spring-boot-starter-test)
    e2e:
      available: false
      tool: null
  coverage:
    available: false
    command: null
  quality:
    linter:
      available: false
      command: null
    type_checker:
      available: false
      command: null
    formatter:
      available: false
      command: null
